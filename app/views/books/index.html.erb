<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'user_info', books: @books %>

  <div class="col-mg-9 offset-md-1">
    <p>title</p>
    <p>Opinion</p>
    <% @books.each do |book| %>
       <p><%= book.title %></p>
       <p><%= book.body %></p>
    <% end %>
  </div>

 </div>
</div>
